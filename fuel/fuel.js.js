import{print, input} from './io_utils'
function main() {
    const fuel = (time * speed)/12
   
    const speed = Number(lines[0])
    const time = Number(lines[1])
    console.log( `${fuel}` );
}
main();

