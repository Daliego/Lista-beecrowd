import { print, input } from "./io_utils.js";

function main() {
    const Worked_hours = Number(input('Workedhours:'))
    const Employee_number = Number(input('Emṕloyee number:'))
    const Money_hour = Number(input('Money/hour is:'))

    const salary = Worked_hours * Money_hour





    print(`The employee number is ${Employee_number} and the salary is ${salary} reals`)





}
main()